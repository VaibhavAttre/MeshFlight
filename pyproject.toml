[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "apps/api/src", "packages/schema/src", "packages/runtime_contracts/src"]
addopts = "-p no:cacheprovider"

[tool.ruff.lint]
select = ["E", "F", "I"]
