# Editor UI

Phase 0 frontend workspace for the React-based operator console and scenario editor.

Initial scope:
- author and reload 2D scenarios
- visualize live topology updates
- call compile and run control-plane APIs

This workspace is dependency-ready but intentionally does not contain MeshFlight feature code yet.
